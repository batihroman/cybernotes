Day 8. Packets and Frames.

What I did today:
- Room "Packets and Frames" in Try Hack Me
- In terminal used command "ip a", and disscussed where is a packet, and where is a frame.
- Understood the difference between a packet and a frame.
- Understood the difference between TCP and UDP.

What I learned today about packets and frames:
- How a packet is encapsulated in a frame.
- That a packet and a frame are a small pieces of data that when encapsulated contain a lot of important information or messages.
- What is the difference between a packet and a frame. 
- What headers a packet uses and what additional info they contain.

What I learned today about TCP and UDP:
- What layers does TCP concists of.
- What the advantages and disadvantages of TCP are.
- What headers do TCP packets have and what info they contain.
- What is a Three-Way-Handshake and what processes it takes to complete it.
- What is a UDP is and what it's advantages and disadvantages are.
- What headers do UDP packets have and what info they contain.
- How the UDP connection is being done and what steps does it take.
- Why it is better to use TCP for messages or recieving important data, and why it is better to use UDP for calls and videos.
 
What I learned today about ports:
- Why ports are so essential in data exchange.
- How many ports are there and how devices decide which port when to use.
- What protocol uses what port number and why.

What can I do after this day:
- Answer to the questions about packets and frames differences.
- In what situation does TCP is better or worse than UDP.
- In terminal show and understand what info is being sent via packet or a frame.
- Tell the difference between ports, and what number of the port is being used in different scenerios.
 

Day 3, 01/03/2026. Linux commands and types of directories.

What I did today: 
-I have completed Linux Fundamentals Part 2.
-Practiced learned command in Ubuntu.
-Ran commands and a test of what I learned today.

What I have learned today:
-Learned how to create files and folders using touch and mkdir
-Understood the difference between cp (copy) and mv (move or rename)
-Used the file command to check the real file type instead of trusting the filename
-Used man to read official documentation for Linux commands
-Learned the basic Linux directory structure:
/etc — the root of the system
/var — system and service data
/root — home directory of the administrator
/tmp — temporary files
-Practiced working inside /tmp as a safe place for experiments
-Tried accessing system directories and understood why a normal user cannot work in /root
-Learned what su does and why it can be dangerous if you don’t understand it 

Commands practiced today:
- “command” —help> list of commands that I can do with this specific command
- touch> create a file
- mkdir> create a directory
- cp> copy file or folder (cp “file name” “new file name”)
- mv> move or rename a file or folder (cp “file name” “new file name”)
- man “command”> opens a manual instruction for usage
- file> shows the type of the file
- su> switch the user (I have to know the username and password)
- su -l > drops us after login straight to the directory of the new user
